{
  "name": "application-parser",
  "version": "0.0.1",
  "description": "Parsing manifest data from apk/ipa file. With typings 🤘",
  "main": "src/index.ts",
  "scripts": {
    "sandbox": "ts-node-dev --respawn sandbox.ts",
    "test": "vitest ./src/*.test.ts",
    "build": "tsc --build"
  },
  "author": "r.segall@tricentis.com",
  "license": "MIT",
  "dependencies": {
    "binary-xml": "^0.0.1",
    "fast-xml-parser": "^4.0.11",
    "node-stream-zip": "^1.15.0"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4",
    "vitest": "^0.25.1"
  }
}
