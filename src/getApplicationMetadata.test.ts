import {describe, it} from "vitest";

describe.concurrent('getApplicationMetadata', () => {
    it('Should fail when the file does not exists', () => {});
    it('Should fail when passing an IOS device but a an apk', () => {});
    it('Should fail when passing an android device but a an ipa', () => {});
    it('Should succeed to process apk file', () => {});
    it('Should succeed to process ipa file', () => {});
})